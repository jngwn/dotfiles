# Keep persistent user files in the standard XDG bases for graphical apps and
# systemd user services. XDG_RUNTIME_DIR remains owned by PAM/systemd per login.
XDG_CONFIG_HOME=${HOME}/.config
XDG_DATA_HOME=${HOME}/.local/share
XDG_STATE_HOME=${HOME}/.local/state
XDG_CACHE_HOME=${HOME}/.cache
