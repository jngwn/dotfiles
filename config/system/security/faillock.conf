# Failed authentication policy for the maintained personal workstation.
# A larger allowance avoids accidental local lockouts while the finite window
# and recovery time still slow repeated authentication attempts.
deny = 10
fail_interval = 900
unlock_time = 300
