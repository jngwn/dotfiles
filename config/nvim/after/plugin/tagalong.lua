-- :TagalongInit / :TagalongDeinit enable or disable paired-tag renames per buffer.
-- Tagalong owns this behavior, so LSP linked editing intentionally remains disabled.
vim.g.tagalong_filetypes = { 'html', 'javascriptreact', 'typescriptreact' }
